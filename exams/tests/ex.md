# Set EX — expansion & cosmology

Labs, in numbering order: `v3_expansion_scout.py` (EX-1..3), `m2_breathing_fabric.py` (EX-4..8), `m3_closed_fabric.py` (EX-9..11), `h1_ball_horizon.py` (EX-12..14). IDs are positional in lab print order; new tests append, never renumber. Queued additions: supernova (1+z) pulse stretch; closed-loop tax (z-rate ~ dS/dt).

| ID | Row | Verifies |
|---|---|---|
| EX-1 | E1 | fixed space FEELS bigger: same hop-count world takes ×2.6 longer to cross as the ledger grows |
| EX-2 | E2 | Hubble law: hop-tax gives redshift z ∝ distance |
| EX-3 | E3 | the fork: tick-tax BLUEshifts (falsified as expansion); uniform tax cancels (invisibility measured) — the bill must bite the message fabric |
| EX-4 | M2a | P10 v2: wells decay as their stuff radiates (gravity is the transient) |
| EX-5 | M2b | global stretch monotone, landing on the uniform prediction (expansion is the residue) |
| EX-6 | M2c | entropy–stretch lockstep (rank correlation 1) — the conversion IS entropy increase |
| EX-7 | M2d | coexistence: deep wells while stretch accumulates (gravity and expansion at once) |
| EX-8 | M2e | bubble-to-fabric signature: clump row overshoots then relaxes; far row only stretches |
| EX-9 | M3a | closed fabric: uniform bath bends NOTHING (forced by Poisson solvability — only contrast bends) |
| EX-10 | M3b | radiation clump bends = matter clump (massless stuff bends while concentrated) |
| EX-11 | M3c | radiation levels into the mean, matter well persists (FRW background/perturbation split in-toy) |
| EX-12 | H1a | linear ledger growth: whole fabric stays reachable (slower, no horizon) |
| EX-13 | H1b | exponential ledger growth: reach saturates at 1/β — a PERMANENT horizon; the edge is a property of the entropy curve |
| EX-14 | H1c | horizons are observer-centered and equal-sized (the edge is relational, not a place) |
| EX-15 | EX15 | supernova stretch: pulse pairs arrive (1+z) apart, exactly c(emit)/c(arrive) — light-curves dilate like spectra shift (tired-light cannot) |
| EX-16 | EX16 | CLOSED LOOP: the tax fed by the real ledger's fold count; fold rate doubles → redshift rate doubles (no script) |
| EX-17 | EX17 | the price audit: energy lost per written bit = constant K·β across epochs — dE = −Kβ·dS, the leak IS the bill |
