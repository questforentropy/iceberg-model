# Set ST — emergent spacetime

Lab: `spacetime_tests.py` (ST-1..4).

**Topology ruling (user, 2026-08-19):** the canonical closed 3D fabric is **S³** (the dimensional-ladder continuation of the measured S² instruments; homogeneous AND isotropic — a flat 3-torus has globally preferred wrap axes, at odds with the no-preferred-frame doctrine; conformally flat, so on the right side of the fakeability theorem). **T³ is the control build, not an assumption** — topology is a dial the exams test, per define-before-sit. Note: no 4th dimension enters the model — S³ is defined intrinsically; embedding coordinates are sprinkler scaffolding only (as 3D coords were for S²) and never appear in the exchange graph. Established (GR-31..33): the β=½ perihelion deviation is NOT a topology/dimension artifact — it survives the honest 3D port with a certified control; global topology is invisible at solar-system scale.

| ID | Row | Verifies |
|---|---|---|
| ST-1 | Q10 | causal cone: reach after k gossip rounds = exactly graph distance k |
| ST-2 | Q11a | PRE-REGISTERED FAILURE: naive block-budget dilation τ = T(1−v) fails Lorentz (the O3 prediction, confirmed) |
| ST-3 | Q11b | sprinkled-DAG longest chain is boost-invariant and linear in the interval (proper time = block count) |
| ST-4 | Q12 | photon chain: null-ish interval carries ~no blocks (light doesn't age) |
| ST-5 | — | *(queued — the S³ port)* the sprinkled S³ self-reports its dimension: ball-volume growth on the kNN graph reads d ≈ 3 from inside (no embedding consulted); the causal sprinkle on it reads d ≈ 3+1 by Myrheim–Meyer order counting |
| ST-6 | — | *(queued — topology blindness)* the local battery is topology-blind: identical local rows (field statics, packet fall, dilation) on matched S³ and T³ patches agree within noise — a LOCAL number that shifts with global topology is a bug, and this row exists to catch it before any real-world comparison |
| ST-7 | — | *(queued — wrap-scale distinguishers)* the two closed worlds differ ONLY at wrap scale: S³ shows antipodal refocus (the SG4 instrument, one rung up) and isotropic return times; T³ shows no refocus and per-axis return times — the preferred-axes tell of the torus, measurable by insiders |
