# Set BH — black holes & archives

Horizon rows (BH-1..3) run on the dumb hole DERIVED by FLU-8's budget dynamics (transport instrument certified by FLU-4). Compute-floor rows (BH-4..6) = the **density route**: packed region → rate → 0 asymptotically, no singularity anywhere in the machine. Archive rows (BH-7..10) are **gated on a history-interior mechanism** (the ball-ledger proposal, a future architecture revision).

*Numbering note (2026-08-19): the archive placeholders formerly listed as BH-4..6 moved to BH-7..9 — they had never appeared in any recorded results table, and positional numbering must match print order. No recorded ID changed.*

| ID | Row | Verifies |
|---|---|---|
| BH-1 | B1 | one-way membrane on the derived hole: launch inside cannot cross the horizon outward; outside launch escapes; outside observer receives nothing |
| BH-2 | B2 | nothing frozen, door open inward: signals from outside cross the horizon INWARD and reach the core in finite time; the budget inside stays alive (contrast with a frozen patch, which blocks both ways) |
| BH-3 | B3 | the hovering pile-up: an outward launch from inside stalls AT the horizon and its amplitude grows — the analog infinite-blueshift signature |
| BH-4 | C1 | the compute floor is asymptotic: tick-scheduler redshift 1+z = 1+L exact; load ladder to 1e300 with the rate strictly positive, finite, monotone — the singularity is continuum language (1+L diverges; the machine never inverts it) |
| BH-5 | C2 | frozen, not sealed: a core signal in an exponentially loaded well ESCAPES in finite time; equal steps deeper multiply the delay by the constant e^(Δ/λ) — the density-route analog of BH-3's log law |
| BH-6 | C3 | the two horizon routes are distinguishable: density hole two-way (escape, arbitrarily slow) vs flux hole one-way (BH-1 membrane) — a real black hole is both at once |
| BH-7 | — | *(archive-gated)* archive intactness: the hole's interior history remains readable in the ledger |
| BH-8 | — | *(archive-gated)* leak-back: new surface blocks referencing interior roots (Hawking-flavored return channel) |
| BH-9 | — | *(archive-gated)* cold-archive economics: storage cost of the hole vs the live fabric |
| BH-10 | — | *(archive-gated, queued)* entropy–area: the hole's information content scales with horizon AREA, not volume (the Bekenstein/holography row — surface = compute screen makes this the ball ledger's native prediction) |
| BH-11 | — | *(queued)* Hawking pairs — the forced-commit mechanism (bubbling-vacuum import, 2026-08-19): the vacuum churns with virtual pairs = fork-and-cancel transactions that re-merge and roll back before ever settling; a horizon makes the rollback IMPOSSIBLE (one partner crossed, append-only forbids the cancel) → the pair is forced to SETTLE — virtual becomes real. Radiation = the horizon forcing pending transactions to commit; the infalling partner posts the debit against the hole's balance (evaporation as double-entry bookkeeping); BH-3's measured surface gravity κ sets the rate. Analog-gravity route (Steinhauer pair-correlation signature); antiparticle = the negative-frequency branch |
| BH-12 | — | *(queued)* the spinning hole: drain-vortex inflow (radial + circulating flux) → ergoregion where the total current exceeds c_g while the radial part is still subcritical; frame dragging forced by the memory theorem (FLU-7); superradiance (Zel'dovich amplification) candidate |
